package Service.FilesReaderWriter;

import Classes.*;
import java.util.*;

public class FilesObjects {
    public static final Map<Integer, Address> addresses = new HashMap<>();
    public static final Map<Integer, Member> members = new HashMap<>();
    public static final Map<Integer, PublishingHouse> publishingHouses = new HashMap<>();
    public static final Map<Integer, Category> categories = new HashMap<>();
    public static final Map<Integer, Author> authors = new HashMap<>();
    public static final Map<Integer, Librarian> librarians = new HashMap<>();
    public static final Map<Integer, Book> books = new HashMap<>();
    public static final Map<Integer, BookItem> bookItems = new HashMap<>();
    public static final Map<Integer, Library> libraries = new HashMap<>();
    public static final Map<Integer, BookBorrowing> borrowings = new HashMap<>();
    public static final Map<Integer, BookReservation> reservations = new HashMap<>();

}
